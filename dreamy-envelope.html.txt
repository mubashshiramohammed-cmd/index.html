<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1.0">
  <title>Dreamy Envelope for Mr. Nazeer</title>

  <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Playfair+Display:wght@400;500;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --pink-light: #ffe8f0;
      --pink-dark: #ffafcc;
      --white: #fff;
      --accent: #ff6fa0;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Playfair Display', serif;
      background: linear-gradient(180deg, var(--pink-light) 0%, var(--white) 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      overflow: hidden;
    }

    .sparkles {
      position: fixed;
      inset: 0;
      pointer-events: none;
      z-index: 0;
    }
    .sparkles span {
      position: absolute;
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: rgba(255,255,255,0.9);
      box-shadow: 0 0 12px rgba(255,255,255,0.8);
      animation: float 10s linear infinite;
    }
    @keyframes float {
      0% { transform: translateY(0) translateX(0); opacity: 1; }
      50% { transform: translateY(-40px) translateX(20px); opacity: .8; }
      100% { transform: translateY(0) translateX(-10px); opacity: 1; }
    }

    .envelope {
      position: relative;
      width: 340px;
      height: 220px;
      perspective: 1200px;
      cursor: pointer;
      z-index: 1;
    }

    .top-flap, .bottom-flap, .letter {
      position: absolute;
      left: 0;
      width: 100%;
      height: 100%;
      transition: all 1s ease;
      transform-style: preserve-3d;
    }

    .bottom-flap {
      background: linear-gradient(to bottom, var(--pink-dark), var(--pink-light));
      border-bottom-left-radius: 6px;
      border-bottom-right-radius: 6px;
      z-index: 1;
    }

    .top-flap {
      background: linear-gradient(to top, var(--pink-dark), var(--pink-light));
      clip-path: polygon(0 0, 50% 100%, 100% 0);
      transform-origin: top;
      z-index: 3;
    }

    .seal {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background: var(--accent);
      width: 40px;
      height: 40px;
      border-radius: 50%;
      box-shadow: 0 0 12px rgba(255,120,170,0.4);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 20px;
      font-family: 'Great Vibes', cursive;
      z-index: 4;
    }

    .letter {
      background: linear-gradient(180deg, #fff 0%, #fff7fa 100%);
      border: 1px solid #ffd9e7;
      border-radius: 4px;
      padding: 24px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.05);
      transform: translateY(100%) scaleY(0);
      z-index: 2;
      overflow-y: auto;
    }

    .envelope.open .top-flap {
      transform: rotateX(-180deg);
    }

    .envelope.open .letter {
      transform: translateY(0) scaleY(1);
      transition-delay: .5s;
    }

    .to {
      position: absolute;
      top: -40px;
      left: 50%;
      transform: translateX(-50%);
      font-family: 'Great Vibes', cursive;
      font-size: 28px;
      color: #b43668;
      text-shadow: 0 1px 0 #fff;
    }

    .click-btn {
      position: absolute;
      bottom: -50px;
      left: 50%;
      transform: translateX(-50%);
      background: var(--accent);
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 20px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      box-shadow: 0 4px 12px rgba(255,110,170,0.3);
    }

    .letter-content {
      font-family: 'Playfair Display', serif;
      font-size: 15px;
      line-height: 1.7;
      color: #5d2b43;
      white-space: pre-wrap;
    }

  </style>
</head>
<body>

  <!-- Sparkles -->
  <div class="sparkles">
    <span style="top:10%; left:20%; animation-duration:12s;"></span>
    <span style="top:30%; left:70%; animation-duration:9s;"></span>
    <span style="top:60%; left:40%; animation-duration:14s;"></span>
    <span style="top:80%; left:10%; animation-duration:11s;"></span>
    <span style="top:50%; left:85%; animation-duration:13s;"></span>
  </div>

  <div class="envelope" id="envelope">
    <div class="to">To: Mr. Nazeer</div>
    <button class="click-btn" id="openBtn">Click Here</button>

    <div class="bottom-flap"></div>
    <div class="top-flap"></div>
    <div class="seal">♥</div>

    <div class="letter">
      <div class="letter-content">
I like you more than I can ever say. With you, I feel a comfort I’ve never felt before—you make me open up, become more myself. I don’t want anyone else to make you feel the way I do, because what I want is only you. Sometimes I become possessive, not out of control, but because I’m scared of losing you.

I know I’ve been harsh about that junior girl, but it was my fear speaking. If you genuinely like someone, I only want you to tell me. No matter what, I’ll always be there for you as a well-wisher, as support, or as someone who deeply cares. More than anything, I wish you success, happiness, and the love you truly want in life.
      </div>
    </div>
  </div>

  <script>
    const envelope = document.getElementById('envelope');
    const openBtn = document.getElementById('openBtn');

    openBtn.addEventListener('click', () => {
      envelope.classList.toggle('open');
    });
  </script>

</body>
</html>
